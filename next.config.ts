import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["ydizojlschamvnddgxyz.supabase.co"], // ← Tambahkan domain ini
  },
};

export default nextConfig;
